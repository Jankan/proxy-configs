# WebRTC STUN/TURN 拦截
# 主要用于减少浏览器通过 WebRTC 暴露真实出口 IP 的风险
# 使用前提：macOS 端需要开启 Surge 增强模式（Enhanced Mode），否则浏览器 UDP/STUN 流量可能不会进入 Surge 规则系统。
# 说明：通过域名关键词和常见 STUN/TURN UDP 端口拦截；如果服务使用非标准域名或端口，仍需浏览器侧配合限制 WebRTC。

AND,((PROTOCOL,UDP),(DEST-PORT,3478))
AND,((PROTOCOL,UDP),(DEST-PORT,5349))
AND,((PROTOCOL,UDP),(DEST-PORT,19302-19309))
DOMAIN-KEYWORD,stun
DOMAIN-KEYWORD,turn
DOMAIN-KEYWORD,webrtc
